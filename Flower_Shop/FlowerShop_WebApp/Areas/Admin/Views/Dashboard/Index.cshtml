@{
    ViewData["Title"] = "Dashboard";
}

<div class="page-header">
    <h1 class="h2">@ViewData["Title"]</h1>
</div>

<div class="row">

    <div class="col-xl-3 col-md-6 mb-4">
        <div class="card stat-card border-left-primary shadow h-100">
            <div class="card-body">
                <div class="row no-gutters align-items-center">
                    <div class="col me-2">
                        <div class="text-xs fw-bold text-primary text-uppercase mb-1">Doanh thu (Completed)</div>
                        <div class="h5 mb-0 fw-bold text-gray-800">@ViewBag.TotalRevenue.ToString("N0") VND</div>
                    </div>
                    <div class="col-auto card-icon">
                        <i class="fas fa-dollar-sign"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="col-xl-3 col-md-6 mb-4">
        <div class="card stat-card border-left-success shadow h-100">
            <div class="card-body">
                <div class="row no-gutters align-items-center">
                    <div class="col me-2">
                        <div class="text-xs fw-bold text-success text-uppercase mb-1">Đơn hàng mới (hôm nay)</div>
                        <div class="h5 mb-0 fw-bold text-gray-800">@ViewBag.NewOrders</div>
                    </div>
                    <div class="col-auto card-icon">
                        <i class="fas fa-clipboard-list"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="col-xl-3 col-md-6 mb-4">
        <div class="card stat-card border-left-warning shadow h-100">
            <div class="card-body">
                <div class="row no-gutters align-items-center">
                    <div class="col me-2">
                        <div class="text-xs fw-bold text-warning text-uppercase mb-1">Đơn hàng chờ xử lý</div>
                        <div class="h5 mb-0 fw-bold text-gray-800">@ViewBag.PendingOrders</div>
                    </div>
                    <div class="col-auto card-icon">
                        <i class="fas fa-comments"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <div class="col-xl-3 col-md-6 mb-4">
        <div class="card stat-card border-left-info shadow h-100">
            <div class="card-body">
                <div class="row no-gutters align-items-center">
                    <div class="col me-2">
                        <div class="text-xs fw-bold text-info text-uppercase mb-1">Người dùng mới (hôm nay)</div>
                        <div class="h5 mb-0 fw-bold text-gray-800">@ViewBag.NewUsers</div>
                    </div>
                    <div class="col-auto card-icon">
                        <i class="fas fa-users"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="alert alert-info mt-3">
    <p class="lead mb-0">Chào mừng bạn đến với trang quản trị! Sử dụng thanh điều hướng bên trái để quản lý các khía cạnh của cửa hàng.</p>
</div>

@section Styles {
    <link rel="stylesheet" href="~/css/dashboard.css" asp-append-version="true" />
}